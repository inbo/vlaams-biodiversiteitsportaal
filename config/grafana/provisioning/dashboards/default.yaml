apiVersion: 1

providers:
  - name: dashboards
    type: file
    updateIntervalSeconds: 30
    options:
      path: /config/config/grafana/provisioning/dashboards
      foldersFromFilesStructure: true