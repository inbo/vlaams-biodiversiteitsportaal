<?xml version="1.0" encoding="UTF-8"?>
<!-- bie-index -->
<Context reloadable="false" useHttpOnly="false">

    <Parameter name="spring.config.additional-location"
               value="file:/config/common/common.yml,file:/config/bie-index/bie-index.yml,${ADDITIONAL_SPRING_CONFIG_LOCATIONS}"
               override="false"/>
    <Parameter name="grails.env"
               value="${GRAILS_ENV}"
               override="false"/>

    <!-- Logging config -->
    <Parameter name="appName"
               value="%blue(bie-index)"
               override="false"/>
</Context>